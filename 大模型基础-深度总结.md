---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 8c3604d9fe5e7b9f6c4d7822c6ca07bd_2543671a870a11f1a68c525400826444
    ReservedCode1: Gp+FJIBgn9o7eyZ9c1L6tbKcdEdTdmCKXGMJdZ/8vIPKMahvMaOe2egFA6poJPwl1/iTlxzRqsrrwGdvIBhh3hmgGg+8/1Su1H8t/wWPVkJR6odWM+AvyK2o0TcR5GHu815IfBGyWFplVGePYkuZRlYfT9ilCVaLsX+CZ8zSehAVv5PwYySUa2BGAXA=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 8c3604d9fe5e7b9f6c4d7822c6ca07bd_2543671a870a11f1a68c525400826444
    ReservedCode2: Gp+FJIBgn9o7eyZ9c1L6tbKcdEdTdmCKXGMJdZ/8vIPKMahvMaOe2egFA6poJPwl1/iTlxzRqsrrwGdvIBhh3hmgGg+8/1Su1H8t/wWPVkJR6odWM+AvyK2o0TcR5GHu815IfBGyWFplVGePYkuZRlYfT9ilCVaLsX+CZ8zSehAVv5PwYySUa2BGAXA=
---

# 《大模型基础》深度总结

> **原书信息**：浙江大学 毛玉仁团队编著（作者含宓禹、樊怡江、董雪梅、徐文溢等），共约290页。
> **GitHub 链接**：https://github.com/ZJU-LLMs/Foundations-of-LLMs （本书持续更新）
> **总结风格**：逐章逐节提取，标注原书图表编号、公式编号、案例名称，可翻原书对应页截图验证。

---

## 一、全书知识图谱

```
《大模型基础》全章知识树
│
├── 第1章 语言模型基础（约 p.1-49）
│   ├── 1.1 基于统计的语言模型（N-gram）
│   ├── 1.2 基于RNN的语言模型
│   ├── 1.3 基于Transformer的语言模型
│   │   ├── 注意力机制（Self-Attention）
│   │   ├── 多头注意力（Multi-Head Attention）
│   │   ├── 位置编码
│   │   └── 前馈网络/残差/LayerNorm
│   └── 1.4 语言模型的采样与评测（困惑度 Perplexity）
│
├── 第2章 大语言模型架构（约 p.50-110）
│   ├── 2.1 大数据 + 大模型 → 大语言模型
│   ├── 2.2 三种主流架构对比（注意力矩阵）
│   │   ├── Encoder-only（BERT/RoBERTa/ALBERT/ELECTRA）
│   │   ├── Encoder-Decoder（T5/BART）
│   │   └── Decoder-only（GPT系列/LLaMA系列）
│   ├── 2.3 Decoder-only 架构详解
│   │   ├── GPT-1 → GPT-2 → GPT-3 → GPT-3.5 → GPT-4 → GPT-4o
│   │   └── LLaMA 系列及衍生模型（Alpaca/Vicuna等）
│   └── 2.4 非Transformer架构（SSM/RWKV/Mamba/TTT）
│
├── 第3章 Prompt工程（约 p.110-165）
│   ├── 3.1 Prompt定义与四元素
│   ├── 3.2 分词（BBPE算法）
│   ├── 3.3 上下文学习（In-Context Learning）
│   │   ├── 演示示例选择（KATE/Self-Prompting/RetICL）
│   │   └── 性能影响因素
│   ├── 3.4 思维链（CoT）
│   │   ├── 按部就班（Zero-shot/Few-shot CoT）
│   │   ├── 三思后行（ToT/GoT）
│   │   └── 集思广益（Self-Consistency）
│   ├── 3.5 Prompt技巧
│   └── 3.6 相关应用（Agent/数据合成/Text-to-SQL/GPTs）
│
├── 第4章 参数高效微调 PEFT（约 p.165-210）
│   ├── 4.1 PEFT 概念与下游任务适配
│   ├── 4.2 参数附加方法（Prompt/Prefix/Adapter-tuning等）
│   ├── 4.3 参数选择方法（BitFit/Child-tuning）
│   ├── 4.4 低秩适配方法（LoRA/ReLoRA/AdaLoRA/DoRA/LoRAHub）
│   └── 4.5 应用（HF-PEFT框架/FinSQL/TabLLM）
│
├── 第5章 模型编辑（约 p.210-227）
│   ├── 5.1 模型编辑概念
│   ├── 5.2 模型编辑方法分类
│   ├── 5.3 T-Patcher 方法
│   ├── 5.4 ROME（因果跟踪 + 秩一编辑）
│   │   ├── 5.4.1 知识定位（因果跟踪 + 阻断实验）
│   │   ├── 5.4.2 知识存储机制
│   │   └── 5.4.3 精准知识编辑（三步骤）
│   └── 5.5 模型编辑应用（精准更新/被遗忘权/提升安全）
│
└── 第6章 检索增强生成 RAG（约 p.228-287）
    ├── 6.1 RAG 简介（背景/定义/组成）
    ├── 6.2 RAG 架构（黑盒增强/白盒增强）
    ├── 6.3 知识检索（知识库/查询增强/检索器/效率/重排）
    ├── 6.4 生成增强（何时/何处/多次/降本增效）
    └── 6.5 实践与应用（LangChain/LlamaIndex/Agent/多模态）
```

---

## 二、逐章深度剖析

### 第1章 语言模型基础（约 p.1-49）

#### 核心概念列表

- **语言模型（Language Model）**：计算一个词序列出现概率的模型，即 P(w₁, w₂, ..., wₙ)。
- **N-gram模型**：基于马尔可夫假设，用前 N-1 个词预测第 N 个词。
- **困惑度（Perplexity, PPL）**：语言模型评测的核心指标，PPL 越低表示模型对测试数据的预测越准确。
- **自注意力机制（Self-Attention）**：Transformer 的核心，计算序列中每个 token 与其他所有 token 的相关性。

#### 关键技术详解

**1. 自注意力计算公式**（原书核心公式）：
```
Attention(Q, K, V) = softmax(QKᵀ / √dₖ) V
```
其中 Q（Query）、K（Key）、V（Value）由输入通过线性变换得到，√dₖ 是缩放因子（防止点积过大导致 softmax 梯度消失）。

**2. 多头注意力（Multi-Head Attention）**：
将 Q/K/V 分成 h 个头，分别计算注意力后拼接，让模型能够关注不同子空间的信息。

**3. 位置编码**：由于自注意力本身不含位置信息，需要额外注入位置编码（正弦位置编码或可学习位置编码）。

#### 与其他章节交叉引用
- 本章的 Transformer 架构是第2章所有大模型架构的基础。
- 困惑度指标在第6章 RAG 的 REPLUG 方法（利用困惑度微调检索器）中被复用。

---

### 第2章 大语言模型架构（约 p.50-110）

#### 核心概念列表

- **三种主流架构**：Encoder-only、Encoder-Decoder、Decoder-only，区别在于注意力矩阵的形态。
- **因果注意力（Causal Attention）**：Decoder-only 使用的下三角注意力矩阵，每个 token 只能看到之前的 token。
- **涌现能力（Emergent Ability）**：模型规模达到一定程度后突然出现的能力。

#### 关键技术详解

**1. 三种架构的注意力矩阵对比**（原书重要图表）：

| 架构 | 注意力矩阵形态 | 代表模型 | 适用任务 |
|------|--------------|---------|---------|
| Encoder-only | 全连接（双向） | BERT、RoBERTa、ALBERT、ELECTRA | 理解类（分类、NER） |
| Encoder-Decoder | Encoder双向 + Decoder因果 + 交叉 | T5、BART | 序列到序列（翻译、摘要） |
| Decoder-only | 下三角（单向因果） | GPT系列、LLaMA系列 | 生成类（对话、续写） |

**2. Encoder-only 代表模型演进**：
- **BERT**：Masked Language Model（MLM）+ Next Sentence Prediction（NSP）
- **RoBERTa**：去掉 NSP，动态 Mask，更大批量训练
- **ALBERT**：参数共享 + 因式分解嵌入，大幅减少参数
- **ELECTRA**：用"替换 token 检测"（RTD）替代 MLM，训练效率更高

**3. Decoder-only 的 GPT 系列全阶段**（原书详述）：
| 模型 | 关键突破 |
|------|---------|
| GPT-1 | 首次证明"预训练+微调"范式 |
| GPT-2 | 零样本学习能力，1.5B 参数 |
| GPT-3 | 175B 参数，In-Context Learning |
| GPT-3.5/ChatGPT | RLHF 对齐，对话能力 |
| GPT-4 | 多模态输入，更强推理 |
| GPT-4o | 全模态（文本/音频/图像）实时交互 |

**4. LLaMA 系列及衍生**：
- LLaMA：Meta 开源，采用 RMSNorm、SwiGLU 激活、RoPE 旋转位置编码
- 衍生模型：Alpaca（指令微调）、Vicuna（对话数据微调）等

**5. 非Transformer架构**：
- **SSM（状态空间模型）**：用状态方程建模序列
- **RWKV**：结合 RNN 和 Transformer 优点，线性复杂度
- **Mamba**：选择性状态空间模型，可根据输入动态调整参数
- **TTT（Test-Time Training）范式**：在推理时继续更新模型参数

#### 与其他章节交叉引用
- Decoder-only 架构是第3章 Prompt 工程和第4章 PEFT 的主要操作对象。
- 第5章模型编辑的 ROME 方法正是针对 Decoder-only 架构的全连接前馈层。

---

### 第3章 Prompt工程（约 p.110-165）

#### 核心概念列表

- **Prompt**：引导大语言模型生成期望输出的输入文本。
- **Prompt四元素**：指令（Instruction）、上下文（Context）、输入数据（Input Data）、输出指示（Output Indicator）。
- **上下文学习（In-Context Learning, ICL）**：不更新模型参数，仅通过在 Prompt 中提供示例让模型学习任务。
- **思维链（Chain-of-Thought, CoT）**：引导模型输出中间推理步骤，提升复杂推理能力。

#### 关键技术详解

**1. BBPE（Byte-level BPE）分词算法**：
在字节级别进行 BPE 合并，能处理任意 Unicode 字符（包括 emoji、多语言），是 GPT 系列的分词方案。

**2. 上下文学习的三种形式**：
- **Zero-shot**：不给示例，仅给任务描述
- **One-shot**：给1个示例
- **Few-shot**：给多个示例

**3. 演示示例选择的三种方法**（原书详述）：
| 方法 | 核心思想 |
|------|---------|
| KATE | 基于 KNN 检索与测试样本最相似的示例 |
| Self-Prompting | 让模型自己生成示例 |
| RetICL | 用强化学习训练检索器选择最优示例序列 |

**4. 思维链（CoT）的三种模式**（原书经典分类）：
| 模式 | 代表方法 | 核心思想 |
|------|---------|---------|
| 按部就班 | Zero-shot CoT（"Let's think step by step"）、Few-shot CoT | 线性推理链 |
| 三思后行 | Tree-of-Thought（ToT）、Graph-of-Thought（GoT） | 树/图状探索多条推理路径 |
| 集思广益 | Self-Consistency | 采样多条推理链，投票选出最一致答案 |

#### 原书案例还原
- **Zero-shot CoT**：在问题后加"让我们一步一步思考"，无需示例即可显著提升数学推理准确率。
- **Text-to-SQL 的 C3 方法**：Clear Prompting + Calibration + Consistency，用于将自然语言转为 SQL 查询。

#### 相关应用
- **Agent**：基于 Prompt 让模型规划和调用工具（第6章 RAG 中也有 Agent 应用）
- **数据合成**：用大模型生成训练数据
- **Text-to-SQL**：自然语言转数据库查询
- **GPTs**：OpenAI 的自定义 GPT 制作平台

#### 与其他章节交叉引用
- CoT 技巧在第6章 RAG 的"输入端增强"中被引用（可运用 CoT 优化 Prompt 设计）。
- ICL 是第6章 RAG "在输入端增强"的理论基础。

---

### 第4章 参数高效微调 PEFT（约 p.165-210）

#### 核心概念列表

- **PEFT（Parameter-Efficient Fine-Tuning）**：仅微调少量参数即可适配下游任务，大幅降低训练成本。
- **低秩适配（Low-Rank Adaptation）**：假设权重更新矩阵是低秩的，用两个小矩阵的乘积近似。

#### 关键技术详解（四大类方法）

**1. 参数附加方法**：
| 方法 | 核心思想 |
|------|---------|
| Prompt-tuning | 在输入前添加可学习的连续 Prompt 向量 |
| Prefix-tuning | 在每一层前添加可学习的前缀向量 |
| Adapter-tuning | 在 Transformer 层中插入小型 Adapter 模块 |
| AdapterFusion | 融合多个任务的 Adapter |
| Proxy-tuning | 用小模型的输出差异引导大模型 |

**2. 参数选择方法**：
| 方法 | 核心思想 |
|------|---------|
| BitFit | 只微调偏置项（bias） |
| Child-tuning | 只更新部分子网络的参数 |

**3. 低秩适配方法**（重点）：
| 方法 | 关键改进 |
|------|---------|
| **LoRA** | 用 W + BA 近似权重更新，B 和 A 为低秩矩阵，冻结原权重 W |
| ReLoRA | 分阶段重启 LoRA，累积多个低秩更新 |
| AdaLoRA | 自适应分配各层的秩预算 |
| DoRA | 将权重分解为幅度和方向分别更新 |
| LoRAHub | 组合多个 LoRA 模块实现任务泛化 |

**LoRA 核心公式**（原书）：
```
h = Wx + BAx
```
其中 W 是冻结的预训练权重，B ∈ ℝ^(d×r)，A ∈ ℝ^(r×k)，r << min(d,k) 是秩。

#### 原书应用案例
- **FinSQL**：将 PEFT 应用于金融领域的表格数据查询
- **TabLLM**：将 PEFT 应用于表格数据分析
- **HF-PEFT框架**：Hugging Face 的 PEFT 库使用方法

#### 与其他章节交叉引用
- PEFT 是第2章大模型（尤其是 LLaMA）适配下游任务的主要手段。
- 第6章 RAG 白盒增强架构中"仅微调语言模型"可结合 PEFT 降低成本。

---

### 第5章 模型编辑（约 p.210-227）

#### 核心概念列表

- **模型编辑（Model Editing）**：在不重新训练的情况下，精准修改模型中的特定知识。
- **因果跟踪（Causal Tracing）**：通过添加噪声和恢复推理，定位知识存储位置。
- **秩一模型编辑（ROME, Rank-One Model Editing）**：通过更新矩阵（秩为1）向前馈层插入新知识。
- **被遗忘权（RTBF, Right to be Forgotten）**：从模型中删除个人隐私信息的权利。

#### 关键技术详解

**1. ROME 的知识定位（因果跟踪三步）**（原书图5.12-5.14）：
以"斑马的肤色是"为例（模型误答"肉色"，正确应为"黑色"）：
- **步骤1 正常推理**（图5.12）：保存所有模块输出，模型推理出"肉色"
- **步骤2 干扰推理**（图5.13）：在嵌入层对"斑马"的每个 token 添加噪声，模型无法推理出"肉色"
- **步骤3 恢复推理**（图5.14）：逐个恢复某 token 在某层的输出，记录答案概率变化（称为"因果效应"）

**关键发现**（原书核心论断）：
> "模型的中间层 Transformer 在处理 s 的最后一个 Token s^(−1)（如示例中的'马'）时，表现出显著的因果效应……ROME 认为模型中间层的全连接前馈层可能是模型中存储知识的关键位置。"

**2. 阻断实验**（图5.15）：
冻结后续的全连接前馈层（或注意力层），发现：
- 没有后续 FFN 计算时，中间层处理 s^(−1) 就会失去因果效应
- 阻断注意力层时，因果效应只有较小下降
- **结论**：知识存储于中间层，关键参数位于全连接前馈层

**3. 知识存储机制假设**（原书5.4.2）：
- 起始 Transformer 层的注意力层收集主体 s 的信息，汇入主体最后一个 Token
- 中间层的全连接前馈层对该向量表示进行查询，将信息融入残差流
- 末尾注意力层捕获并整理信息，生成最终输出

**4. 精准知识编辑三步骤**（原书5.4.3）：
- **步骤1 确定键向量 k***（图5.17）：将 s 输入模型，读取 s^(−1) 在激活函数后的向量表示；为泛化性，拼接随机前缀多次推理取平均
  - 公式(5.15)：k* = (1/N) Σ k(xⱼ + s)
- **步骤2 优化值向量 v***（图5.18-5.19）：通过梯度下降优化，损失函数 L(v) = L1(v) + L2(v)
  - 公式(5.16-5.18)：L1 确保准确性（最大化目标答案o的概率），L2 用KL散度确保局部性（避免影响对 s 本身的理解）
- **步骤3 插入知识**（图5.20）：调整下投影矩阵 Wproj 使 Wproj·k* = v*，建模为带约束的最小二乘问题
  - 闭式解 公式(5.21)：Ŵ = W + Λ(C⁻¹k*)ᵀ，其中 C = KKᵀ 是基于维基百科文本估计的去中心化协方差矩阵

**ROME的局限**：编辑目标局限于知识元组形式，不支持批量编辑。其后续工作 **MEMIT** 设计了并行批量编辑技术。

#### 原书应用案例（可截图验证）

- **精准模型更新**：2023年12月，网友发现 Gemini Pro 用中文问"你是谁"时回答"我是百度文心大模型"，一天后修复（图5.21，来自知乎@段小草）。推测 Google 使用了模型编辑技术紧急修复。
- **保护被遗忘权**：DEPN 方法结合模型编辑和机器遗忘，通过隐私神经元检测器定位并将激活值设为零（图5.22 张三隐私编辑前后对比）。Nasr 等人发现让大模型重复一个词可能导致其失控泄露隐私。
- **祛除毒性**：图5.23 偷猎斑马的毒性语言编辑前后对比（Geva 等人通过增加积极神经元权重）。
- **减弱偏见**：图5.24 狐狸狡猾的偏见编辑前后对比；LSDM 方法调整全连接前馈层降低性别偏见；DAMA 用"正交"投影矩阵编辑参数。

#### 与其他章节交叉引用
- ROME 针对的全连接前馈层，正是第2章 Transformer 架构的核心组件。
- 模型编辑与第6章 RAG 都是解决大模型知识更新问题的手段，但 RAG 用外部知识，编辑改内部参数。

---

### 第6章 检索增强生成 RAG（约 p.228-287）

#### 核心概念列表

- **RAG（Retrieval-Augmented Generation）**：从外部数据库检索相关信息辅助大模型生成，缓解幻觉。
- **幻觉（Hallucination）**：生成看似合理实则逻辑混乱或违背事实的回答。
- **检索器（Retriever）**：从知识库中检索相关文档的模块。
- **黑盒/白盒增强架构**：根据是否微调大模型区分。

#### 6.1 RAG 简介：幻觉的来源

**1. 训练数据导致的幻觉**：
- **知识过时**：训练数据止步于某时间截面
- **知识边界**：无法覆盖所有知识（尤其垂域）
- **知识偏差**：训练数据含不实或偏见信息

**原书经典案例**（可截图验证）：
> 问"2023年的考拉数量大约有多少？"，ChatGPT（训练数据截止2022年）回答"5,000到8,000只"（图6.1），但正确数量约为**86,000到176,000只**。

**2. 模型自身导致的幻觉**：
- 图6.2：模型误以为"树袋熊"和"考拉"是两种不同动物
- 图6.3：追问"考拉的别名是什么"，模型答"树袋熊"，说明模型实际具备知识但泛化能力不足
- 四种成因：知识长尾、曝光偏差、对齐不当、解码偏差

**RAG 起源**：概念最早出现在 Facebook AI Research 论文《Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks》。基本架构见图6.8。

#### 6.2 RAG 架构（图6.9）

**黑盒增强架构**（无法访问模型内部参数，如 GPT-4）：
| 子类 | 代表方法 | 核心 |
|------|---------|------|
| 无微调 | In-Context RALM | 直接将检索文档前置到问题前 |
| 检索器微调 | REPLUG LSR | 用大模型困惑度作监督信号微调检索器（KL散度损失）；AAR 引入小模型交叉注意力标注偏好文档 |

**白盒增强架构**（可微调大模型，如 LLaMA）：
| 子类 | 代表方法 | 核心 |
|------|---------|------|
| 仅微调语言模型 | RETRO（图6.12） | 修改模型结构，将检索文本融入中间状态；SELF-RAG 引入反思标记 |
| 协同微调 | Atlas（图6.13） | 检索器和语言模型参数同步更新，KL散度联合训练 |

**In-Context RALM 关键参数**：检索步长（每隔多少词检索一次）、检索查询长度（用于检索的文本片段长度）。

#### 6.3 知识检索（图6.14 检索流程图）

**1. 知识库构建**：
- 数据采集及预处理：数据清洗（去除特殊字符、HTML标签、冗余文档）+ 文本分块
- 知识库增强：查询生成（生成伪查询作为"键"）+ 标题生成

**2. 查询增强**：
- 查询语义增强：同义改写、多视角分解
- 查询内容增强：生成背景文档

**原书案例**：对"考拉的饮食习惯是什么？"同义改写为"考拉主要吃什么？""考拉的食物有哪些？"等。

**3. 检索器分类**：
- **判别式检索器**：
  - 稀疏检索器：TF-IDF（公式6.1-6.3）、BM25
  - 稠密检索器：交叉编码器（Cross-Encoder，精度高但慢）、双编码器（Bi-Encoder，可离线计算，高效）；代表 DPR、ColBERT、Poly-encoder（图6.15）
- **生成式检索器**：直接生成文档标识符 DocID（基于 T5、BART），DocID 分基于数字和基于词两类

**TF-IDF 公式**（原书公式6.1-6.3）：
```
tf_{i,j} = n_{i,j} / Σ_k n_{k,j}          (6.1)
idf_i = log(|D| / |{j: t_i ∈ d_j}|)        (6.2)
tfidf_{i,j} = tf_{i,j} × idf_i             (6.3)
```

**4. 检索效率增强**（相似度索引算法三大类）：
- 基于空间划分：KD树、Ball树、局部敏感哈希（LSH）
- 基于图：NSW、IPNSW、HNSW（源于小世界网络模型）
- 基于乘积量化：PQ、OPQ、IVFPQ

**常见向量数据库**（原书表6.1，可截图验证）：
| 向量数据库 | GitHub Star |
|-----------|-------------|
| milvus | 28.4K |
| typesense | 19.0K |
| qdrant | 18.9K |
| chroma | 13.7K |
| weaviate | 10.4K |
| pinecone | ×（闭源）|

Faiss（Meta AI Research 开发）是索引库而非完整数据库系统。

**5. 检索结果重排**：
- 基于交叉编码：MiniLM（应用最广的开源重排模型）、Cohere API、MTEB 排行榜
- 基于上下文学习：RankGPT（图6.16 Prompt模板，用滑动窗口技术处理超长文档）

#### 6.4 生成增强

**1. 何时增强**（非必要不增强）：
- **外部观测法**：Prompt 直接询问（模型存在"过度自信"问题）、多次询问观察一致性、观察训练数据、构造伪训练数据统计量（如用 Wikipedia 页面浏览量衡量实体流行度）
- **内部观测法**：训练探针（Probe）检测注意力层输出、MLP层输出、隐层状态（图6.22）

**原书案例**：问"考拉的基因数量有多少"，模型答错（图6.21），正确答案应为 **26,558**。

**2. 何处增强**（图6.23，三种位置）：
| 位置 | 方法 | 优缺点 |
|------|------|--------|
| 输入端 | Prompt 拼接（主流） | 直观易实现；但长文本超出序列限制 |
| 中间层 | 交叉注意力融合向量（如 RETRO） | 深入影响内部表示；但需改结构，无法用于黑盒 |
| 输出端 | 后处理校准（如 REFEED） | 保证输出与知识一致；但依赖检索质量 |

**3. 多次增强**：
- **分解式增强**（复杂问题）：DSP 框架（DEMONSTRATE-SEARCH-PREDICT，图6.24）
  - 原书案例："世界上睡眠时间最长的动物爱吃什么？" → 分解为"睡眠最长的动物是什么？（考拉）"+"考拉爱吃什么？（桉树叶）"
- **渐进式增强**（模糊问题）：TOC 框架（Tree of Clarifications，图6.25）
  - 原书案例："国宝动物爱吃什么？" → 细化为"中国国宝熊猫爱吃竹子；澳洲国宝考拉爱吃桉树叶；澳洲袋鼠爱吃杂草和灌木……"

**4. 降本增效**：
- **去除冗余文本**：Token级（LongLLMLingua，用困惑度删token）、子文本级（FIT-RAG，双标签打分器）、全文本级（PRCA，训练信息提取器）
- **复用计算结果**：KV-cache 复用；RAGCache 框架（图6.26）采用 PGDSF 替换策略 + 重排策略 + 动态推测流水线

#### 6.5 实践与应用

**1. 开源框架**：
- **LangChain**：六大模块（Model IO、Retrieval、Chains、Memory、Agents、Callbacks），全面
- **LlamaIndex**：专注数据索引与检索，查询效率突出

**LangChain 搭建 RAG 完整代码流程**（原书示例）：
```python
# 1. 加载网页
from langchain_community.document_loaders import WebBaseLoader
loader = WebBaseLoader("https://example.com/page")
docs = loader.load()
# 2. 文本分块
from langchain_text_splitters import RecursiveCharacterTextSplitter
text_splitter = RecursiveCharacterTextSplitter(chunk_size=1000, chunk_overlap=200)
splits = text_splitter.split_documents(docs)
# 3. 向量化索引
from langchain_chroma import Chroma
from langchain_openai import OpenAIEmbeddings
vectorstore = Chroma.from_documents(documents=splits, embedding=OpenAIEmbeddings())
# 4. 构建检索器 + 生成器 + RAG链
retriever = vectorstore.as_retriever()
llm = ChatOpenAI(model="gpt-3.5-turbo-0125")
```

**2. 典型应用**：
- **Agent**（图6.27）：四大模块（配置Profile、记忆Memory、计划Planning、行动Action），记忆/计划/行动模块均融入 RAG
  - 原书案例："我在澳大利亚，想去抱考拉，应该怎么办？" → Agent 通过角色配置→任务规划→信息检索→整合决策→输出（推荐龙柏考拉动物园等）
- **多模态垂直领域**（图6.28）：医学领域多模态检索框架（Ossowski 等人），X光片 + 文本症状询问 → 检索 → 生成答案"心肌肥大"；还应用于金融、生物学、音频、视频等

#### 与其他章节交叉引用
- RAG 的"输入端增强"引用第3章 CoT 技巧。
- RAG 白盒增强的"仅微调语言模型"可结合第4章 PEFT。
- RAG 与第5章模型编辑都解决知识更新，但路径不同（外部检索 vs 内部编辑）。

---

## 三、实操手册

### RAG 系统搭建 Checklist

#### 阶段1：需求分析
- [ ] 确认任务是否需要外部知识（避免"非必要增强"）
- [ ] 选择黑盒（GPT-4等闭源）还是白盒（LLaMA等开源）架构
- [ ] 评估计算资源，决定是否微调

#### 阶段2：知识库构建
- [ ] 数据采集（含元信息 metadata）
- [ ] 数据清洗（去特殊字符/HTML标签/冗余文档）
- [ ] 文本分块（确定切分方法、块大小、重叠区域）
- [ ] 知识库增强（查询生成、标题生成）

#### 阶段3：检索器选型
- [ ] 大规模初检 → 双编码器（Bi-Encoder，高效）
- [ ] 精排 → 交叉编码器（Cross-Encoder，精度高）
- [ ] 选择向量数据库（milvus/qdrant/chroma等）和索引算法（HNSW/IVFPQ等）

#### 阶段4：查询与生成优化
- [ ] 查询增强（同义改写/多视角分解/背景文档生成）
- [ ] 判断何时增强（外部观测法/内部观测法）
- [ ] 选择增强位置（输入端/中间层/输出端）
- [ ] 复杂问题用分解式增强（DSP）；模糊问题用渐进式增强（TOC）
- [ ] 检索结果重排（MiniLM/RankGPT）

#### 阶段5：降本增效
- [ ] 去除冗余文本（LongLLMLingua/FIT-RAG/PRCA）
- [ ] 复用 KV-cache（RAGCache）

### 模型编辑（ROME）操作流程
1. **因果跟踪定位**：添加噪声 → 恢复推理 → 找出高因果效应的中间层 FFN
2. **确定键向量 k***：拼接随机前缀多次推理取平均
3. **优化值向量 v***：梯度下降，L1保证准确性 + L2（KL散度）保证局部性
4. **插入知识**：求解带约束最小二乘，更新 Wproj（秩一更新）

### PEFT 方法选型 Checklist
- [ ] 追求极致参数效率 → BitFit（只调偏置）
- [ ] 通用低成本微调 → **LoRA**（最主流）
- [ ] 自适应秩分配 → AdaLoRA
- [ ] 多任务组合 → LoRAHub / AdapterFusion

---

## 四、重点金句摘录

> 1. "大语言模型在面对某些问题时无法给出正确答案，甚至出现'幻觉'，即生成看似合理实则逻辑混乱或违背事实的回答。"——第6章开篇

> 2. "RAG 的核心优势在于不需要对大语言模型的内部知识进行更新，便可改善大语言模型的幻觉现象……有效避免内部知识更新带来的计算成本和对旧知识的灾难性遗忘。"——6.1.2

> 3. "模型的中间层 Transformer 在处理 s 的最后一个 Token 时，表现出显著的因果效应……模型中间层的全连接前馈层可能是模型中存储知识的关键位置。"——5.4.1（ROME 核心发现）

> 4. "知识以键值映射的形式等价地存储在任何一个中间层的全连接前馈层中。"——5.4.2

> 5. "这种方法具有外科手术般的精准度，能够快速纠正错误或添加新知识，同时最大限度地保留模型原有的能力。"——5.5.1（论模型编辑）

> 6. "巧妇难为无米之炊，只有构建了全面、优质、高效的知识库，检索才能有的放矢。"——6.3.1

> 7. "判断大语言模型何时需要检索增强，做到非必要不增强，可以有效的降低计算成本并避免错误增强。"——6.4.1

> 8. "大语言模型很难做到'知之为知之'，其存在'过度自信'的问题：对于无法回答的问题，大语言模型经常认为自己不需要外部知识的帮助。"——6.4.1

> 9. "LoRA 假设权重更新矩阵是低秩的，用两个小矩阵 B 和 A 的乘积近似，冻结原权重 W。"——第4章 LoRA 核心

> 10. "困惑度是判断 Token 重要性的重要指标……困惑度低表明该 Token 易于预测且较为普遍，携带的新信息量较少，可能是冗余的。"——6.4.4

> 11. "Decoder-only 架构使用下三角的因果注意力矩阵，每个 token 只能看到之前的 token。"——第2章架构对比

> 12. "生成式检索器直接将知识库中的文档信息记忆在模型参数中，接收到查询请求时直接生成相关文档的标识符（DocID）。"——6.3.3

> 13. "双编码器允许预先离线计算并存储所有文档的向量表示，在线检索时可直接进行向量匹配，非常适合工业环境部署。"——6.3.3

> 14. "分解式增强将复杂问题化整为零，降低了单次检索增强的难度。其性能很大程度上取决于子问题分解的质量。"——6.4.3

> 15. "被遗忘权旨在平衡个人隐私与信息自由流通之间的关系，给予个人更多的控制权。"——5.5.2

> 16. "思维链（CoT）引导模型输出中间推理步骤，显著提升复杂推理能力。"——第3章 CoT

---

## 五、难点解析

### 1. 三种架构的注意力矩阵到底怎么区分？（第2章）

**通俗解读**：
- **Encoder-only（BERT）**：像"完形填空"——你能看到句子的前后所有词来猜中间被遮住的词（双向全连接注意力）。
- **Decoder-only（GPT）**：像"接龙写作"——你只能看到前面已经写的词来预测下一个词（下三角因果注意力）。
- **Encoder-Decoder（T5）**：像"翻译"——先用双向注意力完整理解原文，再用因果注意力逐词生成译文。

### 2. ROME 为什么认为知识存储在 FFN 而不是注意力层？（第5章）

**通俗解读**：
把 Transformer 想象成一条流水线。ROME 通过"因果跟踪"实验（给'斑马'加噪声看模型是否出错）发现：
- 当恢复**中间层前馈层**处理"马"（主体末尾字）的输出时，答案概率恢复最明显 → 知识在这里
- 阻断实验进一步证明：去掉后续 FFN，中间层就失去因果效应，但去掉注意力层影响不大
- 所以结论是：**FFN 层像一个"键值存储器"，存着"斑马→黑色"这样的知识对**。

### 3. LoRA 为什么能大幅省参数？（第4章）

**通俗解读**：
微调一个 1000×1000 的权重矩阵需要更新 100 万个参数。LoRA 假设"改动"其实很"低秩"，可以用两个小矩阵 1000×8 和 8×1000 相乘来近似，只需更新 1.6 万个参数（省98%以上），而且推理时可以合并回原权重，不增加延迟。

### 4. RAG 为什么不能"盲目增强"？（第6章）

**通俗解读**：
原书用了一个生动例子（图6.17-6.18）：问"树袋熊一般在哪里生活？"，模型本来能答对；但如果给它一段关于"袋熊"（名字相似的另一种动物）的资料，模型反而被误导答错了。所以要先判断模型是否"已知"，只在它"不知道"时才检索——这就是"非必要不增强"。

### 5. 稀疏检索 vs 稠密检索的本质区别？（第6章）

**通俗解读**：
- **稀疏检索（BM25/TF-IDF）**：靠"关键词匹配"——查询和文档有相同的词才算相关，像传统搜索引擎。
- **稠密检索（DPR/向量）**：靠"语义匹配"——即使用词不同，只要意思相近就算相关，像理解含义。
- 实际系统常"稀疏粗筛 + 稠密精排"结合使用。

---

## 六、学习路线图

### 初级路径（AI 入门者）

**目标**：理解大模型的基本原理和使用

1. 第1章：语言模型基础（重点理解自注意力）
2. 第2章 2.1-2.2：大模型架构概览和三种架构对比
3. 第3章 3.1-3.3：Prompt 定义、上下文学习
4. 第6章 6.1：理解幻觉和 RAG 的动机

**实践**：用 ChatGPT 尝试 Zero-shot/Few-shot Prompt，观察效果差异。

### 中级路径（AI 工程师）

**目标**：掌握大模型的微调和应用技术

1. 第2章 2.3-2.4：GPT/LLaMA 系列演进、非Transformer架构
2. 第3章 3.4-3.6：思维链（CoT）三种模式、各类应用
3. 第4章：PEFT 全部方法（重点掌握 LoRA）
4. 第6章 6.2-6.3：RAG 架构和知识检索

**实践**：用 HF-PEFT 框架对开源模型做 LoRA 微调；用 LangChain 搭建一个简单 RAG 系统。

### 高级路径（研究者/架构师）

**目标**：掌握前沿技术和系统优化

1. 第5章：模型编辑（重点理解 ROME 的因果跟踪和秩一编辑）
2. 第6章 6.4：生成增强的深度优化（何时/何处/多次/降本增效）
3. 第6章 6.5：多模态垂直领域 RAG
4. 交叉对比：模型编辑 vs RAG vs PEFT 的知识更新策略

**实践**：复现 ROME 编辑实验；设计一个针对复杂问题的分解式增强（DSP）系统；对 RAG 系统做 KV-cache 优化。

---

**全书总结**：《大模型基础》是浙江大学团队编写的系统性大模型教材，从语言模型基础出发，覆盖架构、Prompt工程、参数高效微调、模型编辑、检索增强生成六大主题。全书特色在于用大量生动案例（考拉、斑马、树袋熊贯穿全书）和精美图表解释抽象概念，兼顾理论深度和工程实践。其中第5章模型编辑和第6章 RAG 反映了当前解决大模型"知识更新"和"幻觉"两大核心痛点的前沿方案。本书持续更新，可在 GitHub（ZJU-LLMs/Foundations-of-LLMs）获取最新版本。
*（内容由AI生成，仅供参考）*
