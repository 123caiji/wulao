---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 8c3604d9fe5e7b9f6c4d7822c6ca07bd_53a69626870811f1b66e525400e6dd8f
    ReservedCode1: TqGvS5KVkIhLDyWt77rUEusYpgmvI3UYn9CLGByKQYLnbNjtDylhFaYLBSWVBdBf1mn6AjAW0+1h/Yv6cDCGjQ5ODDlPCRI0Z2QV1fxVAwr14qpGk4owRxEjiRBij9g0CniWp/xEcB2tIu1+ozpuLVlBtxLNbdfUd/CyOLQJSM5K0yVbaKGRGHQtXTc=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 8c3604d9fe5e7b9f6c4d7822c6ca07bd_53a69626870811f1b66e525400e6dd8f
    ReservedCode2: TqGvS5KVkIhLDyWt77rUEusYpgmvI3UYn9CLGByKQYLnbNjtDylhFaYLBSWVBdBf1mn6AjAW0+1h/Yv6cDCGjQ5ODDlPCRI0Z2QV1fxVAwr14qpGk4owRxEjiRBij9g0CniWp/xEcB2tIu1+ozpuLVlBtxLNbdfUd/CyOLQJSM5K0yVbaKGRGHQtXTc=
---

# 《大模型基础》深度总结提炼

> **来源**：浙江大学开源教材 | **核心内容**：从语言模型基础到大模型全栈技术
> **篇幅**：6章，290页 | **定位**：大语言模型入门到进阶的系统性教材

---

## 一、书籍概览

本书系统性地覆盖了大语言模型（LLM）的核心技术栈，从统计语言模型的数学基础出发，贯穿 Transformer 架构演进、Prompt 工程、参数高效微调、模型编辑以及检索增强生成（RAG）六大主题。每章均配有详实的公式推导、算法伪代码和前沿论文引用，适合高年级本科生、研究生及 ML 从业者。

全书特色在于**理论与实践并重**：既讲清楚"是什么"和"为什么"，也通过大量案例展示"怎么用"。

---

## 二、核心知识体系与章节结构

### 第1章：语言模型基础

#### 1.1 从 n-gram 到神经网络语言模型

| 发展阶段 | 代表方法 | 核心思想 | 局限 |
|----------|---------|---------|------|
| 统计语言模型 | n-gram | 马尔可夫假设 | 数据稀疏、维度灾难 |
| 前馈神经网络 | FFNN-LM | 词向量+全连接网络 | 上下文窗口固定 |
| 循环神经网络 | RNN-LM | 循环连接建模任意长序列 | 梯度消失、推理慢 |
| Transformer | GPT/BERT | 自注意力全局建模、并行计算 | 计算复杂度O(n^2) |

#### 1.2 核心数学概念

- **最大似然估计（MLE）**：语言模型训练的理论基础
- **困惑度（Perplexity, PPL）**：PPL = exp(cross-entropy loss)，越低越好
- **温度采样**：通过温度参数tau控制生成多样性，tau下降 -> 更确定，tau上升 -> 更多样

#### 1.3 解码策略对比

| 策略 | 方法 | 特点 | 适用场景 |
|------|------|------|---------|
| 贪心搜索 | 每步取最高概率 | 快速但易陷入局部最优 | 对多样性要求低 |
| 束搜索（Beam Search） | 每步保留Top-k条路径 | 平衡质量与多样性 | 翻译、摘要 |
| Top-k采样 | 从Top-k个候选词中随机采样 | 增加多样性 | 对话、创意写作 |
| Top-p（核采样） | 从累积概率>=p的最小词集中采样 | 动态适应概率分布 | 通用生成 |

#### 1.4 语言模型评测

- **内在评测**：PPL、BPW（Bits Per Word）
- **外在评测**：GLUE/SuperGLUE、MMLU、HumanEval、SQuAD等下游任务
- **人工评测**：流畅性、一致性、有用性三维评分

---

### 第2章：大语言模型架构演进

#### 2.1 缩放法则（Scaling Laws）

> **核心发现**：模型性能与参数量N、数据量D、计算量C遵循幂律关系。

- **Kaplan等（2020）**：在计算预算固定时，大模型+少数据 优于 小模型+多数据
- **Chinchilla定律**：对于给定的计算预算，最优策略是等比例扩大模型和数据（N 正比于 D）
- **涌现能力**：当模型规模达到一定阈值时，某些能力（如算术推理、思维链）突然出现

#### 2.2 三大架构范式

| 架构类型 | 代表模型 | 核心机制 | 擅长任务 |
|----------|---------|---------|---------|
| **Encoder-only** | BERT, RoBERTa, DeBERTa | 双向注意力，Masked LM预训练 | 文本理解、分类、NER、问答 |
| **Encoder-Decoder** | T5, BART, GLM | 编码器+解码器，Span Corruption | 翻译、摘要、生成式问答 |
| **Decoder-only** | GPT系列, LLaMA, PaLM | 单向注意力，自回归预训练 | 开放域对话、代码生成、通用推理 |

#### 2.3 GPT系列演进

| 版本 | 参数量 | 核心创新 |
|------|-------|---------|
| GPT-1 (2018) | 117M | 无监督预训练+有监督微调范式 |
| GPT-2 (2019) | 1.5B | 展示零样本泛化能力 |
| GPT-3 (2020) | 175B | 上下文学习(ICL)成为新范式 |
| GPT-4 (2023) | 未公开 | 多模态输入、更强的推理与安全对齐 |

#### 2.4 LLaMA系列

Meta开源大模型的里程碑：
- **LLaMA 1**（7B-65B）：纯公开数据训练，挑战"数据越多越好"假设
- **LLaMA 2**：引入RLHF，发布对话版本
- **LLaMA 3**：扩大训练数据至15T tokens，性能接近闭源顶尖模型

#### 2.5 非Transformer架构探索

| 架构 | 核心思想 | 代表作 |
|------|---------|-------|
| **状态空间模型（SSM）** | 替代注意力，线性复杂度 | Mamba, S4 |
| **RWKV** | RNN+Transformer混合，线性推理 | RWKV-v5 |
| **RetNet** | 多尺度保留机制，O(1)推理 | RetNet |
| **MoE（混合专家）** | 稀疏激活，大参数量+低计算量 | Mixtral 8x7B, Switch Transformer |

---

### 第3章：Prompt 工程

#### 3.1 上下文学习（In-Context Learning, ICL）

> **定义**：模型在推理时通过Prompt中的示例"临时学会"任务，无需更新参数。

**ICL工作机制**（三种主流假说）：

| 假说 | 核心观点 | 代表工作 |
|------|---------|---------|
| 贝叶斯推断假说 | 示例诱导模型推断任务的"隐变量" | Xie et al. 2022 |
| 梯度下降假说 | 注意力机制等价于隐式梯度更新 | Dai et al. 2023 |
| 任务向量假说 | ICL在隐空间生成指向特定任务的"任务向量" | Hendel et al. 2023 |

#### 3.2 ICL示例选择策略

| 策略 | 方法 | 效果提升 |
|------|------|---------|
| 随机选择 | 从训练集随机抽k个示例 | 基线 |
| KATE（KNN检索） | 检索最相似的示例 | +5%~10%准确率 |
| 动态选择 | 为每个测试样本独立选择最合适示例 | +3%~8% |
| 排序优化 | 按难度递增或相似度递减排列 | +2%~5% |

#### 3.3 思维链（Chain-of-Thought, CoT）

> **定义**：在Prompt中引导模型逐步推理，而非直接输出答案。

| CoT变体 | 方法 | 适用场景 |
|----------|------|---------|
| Zero-shot CoT | 添加"Let us think step by step" | 通用推理 |
| Few-shot CoT | 提供带推理步骤的示例 | 复杂数学/逻辑题 |
| Self-Consistency | 多次采样+投票，选最多出现的答案 | 提高推理可靠性 |
| Tree-of-Thought (ToT) | 树状搜索多个推理路径 | 规划/博弈类问题 |
| Graph-of-Thought (GoT) | 图结构组织思路，允许多路合并 | 写作、多约束问题 |

#### 3.4 Prompt设计技巧清单

1. **角色扮演**：赋予模型特定角色（"你是一位资深算法工程师"）
2. **格式约束**：明确输出格式（JSON、Markdown、列表）
3. **分步引导**：将复杂任务拆解为子步骤
4. **反面示例**：提供不期望的输出作为对照
5. **情绪锚定**：添加紧急语气词可激发不同输出风格

#### 3.5 Prompt在Agent中的应用

Agent四大模块中的Prompt角色：
- **配置（Profile）**：角色定义Prompt
- **记忆（Memory）**：历史对话摘要Prompt
- **计划（Planning）**：任务分解Prompt（含ReAct范式）
- **行动（Action）**：工具调用Prompt（Function Calling）

---

### 第4章：参数高效微调（PEFT）

#### 4.1 PEFT三类范式

```
参数高效微调（PEFT）
|-- 参数附加方法（Adapter-based）
|   |-- Adapter：在Transformer层间插入可训练小模块
|   |-- Prompt-tuning / Prefix-tuning：在输入或每层添加可学习向量
|   |-- AdapterFusion：多Adapter知识融合
|   |-- Proxy-tuning：用代理模型引导目标模型行为
|
|-- 参数选择方法（Selection-based）
|   |-- BitFit：仅微调偏置项（bias）
|   |-- Child-tuning：仅微调子网络中的部分参数
|
|-- 低秩适配方法（LoRA-based）【最主流】
    |-- LoRA：对权重增量做低秩分解 DeltaW = BA
    |-- AdaLoRA：自适应分配不同模块的秩
    |-- DoRA：分解为方向+幅度学习
    |-- ReLoRA：训练过程中动态重启动低秩分解
    |-- LoRAHub：多个LoRA模块的即插即用组合
```

#### 4.2 LoRA核心原理（深度解析）

**数学表达**：
- 原始前向：h = W0 * x
- LoRA修改：h = W0 * x + BA * x，其中 B in R^{d x r}, A in R^{r x k}, r << min(d, k)

**关键设计决策**：

| 设计要素 | 推荐配置 | 说明 |
|---------|---------|------|
| 秩r | 8-64 | r=8通常足够，复杂任务可用64 |
| 应用位置 | Q, V（注意力矩阵） | 仅作用于注意力层的Q和V矩阵即可 |
| 缩放因子alpha | alpha=2r | 与r成比例 |
| Dropout | 0.05-0.1 | 轻微正则化 |

**LoRA的优势**：
- 可训练参数仅占原模型的0.1%-1%
- 多任务切换零开销（换LoRA权重即可）
- 无推理延迟（可合并到原权重 W = W0 + BA）
- 梯度内存占用大幅降低

#### 4.3 PEFT实践

- **Hugging Face PEFT库**：统一接口支持LoRA/Adapter/Prompt-tuning/Prefix-tuning
- **典型案例**：
  - **TabLLM**：用LoRA将LLM适配表格数据任务
  - **FinSQL**：用PEFT将通用LLM适配Text-to-SQL

---

### 第5章：模型编辑

#### 5.1 模型编辑的定义与五维性质

| 性质 | 定义 | 评估指标 |
|------|------|---------|
| **准确性（Reliability）** | 编辑后的知识能被正确召回 | 成功率 |
| **泛化性（Generalization）** | 同义改写后仍正确 | 改写测试成功率 |
| **可迁移性（Portability）** | 编辑一个事实影响相关的推理 | 下游推理准确度 |
| **局部性（Locality）** | 不影响无关知识 | 无关样本性能保持 |
| **高效性（Efficiency）** | 编辑的速度和资源开销 | 耗时/GPU显存 |

#### 5.2 三类编辑方法

| 类别 | 方法 | 核心思想 | 代表工作 |
|------|------|---------|---------|
| **外部拓展法** | 记忆增强 | 将新知识存在外部记忆，推理时检索 | SERAC, T-Patcher |
| **内部修改法（元学习）** | 训练编辑器 | 用超网络预测梯度更新 | KE, MEND |
| **内部修改法（定位编辑）** | 因果干预 | 定位知识存储神经元->修改 | KN, **ROME**, MEMIT |

#### 5.3 ROME方法深度解析（本章重点）

**三步走流程**：

1. **因果跟踪实验（Causal Tracing）**：
   - 通过"破坏+恢复"实验，逐层定位MLP中间层是事实知识存储的关键位置
   - 破坏（Corrupt）：替换主语嵌入 -> 观察各层激活变化
   - 恢复（Restore）：逐层恢复干净激活 -> 观察答案概率恢复程度

2. **阻断实验**：
   - 验证特定MLP层在"主语->宾语"联想中的唯一作用
   - 结论：事实关联存储在中层的MLP中

3. **键值更新**：
   - 将事实视为 (主语, 关系, 宾语) 三元组
   - 通过求解约束优化问题更新MLP权重，使模型输出目标宾语

#### 5.4 模型编辑的应用场景

| 场景 | 说明 |
|------|------|
| **精准知识更新** | 时效性知识修正（如"现在XX的主席是谁？"） |
| **隐私保护** | 从模型中删除特定个人的信息 |
| **祛除毒性** | 移除有害/偏见生成关联 |
| **减弱偏见** | 纠正性别/种族等刻板印象 |

---

### 第6章：检索增强生成（RAG）

#### 6.1 RAG背景与架构

**为什么需要RAG？** LLM的三大痛点：
1. **幻觉（Hallucination）**：生成的事实性错误
2. **知识截止**：训练数据时效性限制
3. **长尾知识不足**：低频知识难以覆盖

**RAG标准架构**：

```
用户查询 -> 检索器 -> 知识库 -> 相关文档 -> 增强生成 -> 大语言模型 -> 答案
```

#### 6.2 RAG架构分类

| 类型 | 方法 | 核心思想 |
|------|------|---------|
| **黑盒增强** | In-Context RALM | 检索->拼接->输入黑盒LLM |
| | REPLUG + LSR | 多文档检索+集成打分 |
| **白盒增强** | RETRO | 交叉注意力融合检索文档的Chunk表示 |
| | Atlas | 联合训练检索器和生成器 |

#### 6.3 知识检索全流程

##### 6.3.1 知识库构建
- **数据采集**：多源文档、PDF、网页、数据库
- **预处理**：文本清洗、格式统一、去重
- **数据增强**：问题-答案对生成、同义改写

##### 6.3.2 查询增强
- **语义增强**：对模糊查询进行细化
- **内容增强**：基于历史对话补充上下文

##### 6.3.3 检索器分类

| 类型 | 方法 | 原理 | 代表模型 |
|------|------|------|---------|
| **稀疏检索** | TF-IDF | 词频-逆文档频率 | - |
| | BM25 | 概率相关性框架 | - |
| **稠密检索** | 双编码器（Bi-Encoder） | 查询和文档分别编码为向量，内积算相似度 | DPR |
| | ColBERT | 晚交互（Late Interaction），Token级匹配 | ColBERT |
| **生成式检索** | 直接生成文档ID | DSI（Differentiable Search Index） | NCI |

##### 6.3.4 检索效率增强

| 方法 | 算法 | 原理 |
|------|------|------|
| 空间划分 | KD-Tree, Ball Tree, Annoy, IVFPQ | 向量空间分治 |
| 图方法 | HNSW, NSG | 构建导航图，贪心搜索 |
| 量化 | 乘积量化（PQ）, OPQ | 将高维向量压缩为低比特编码 |

##### 6.3.5 主流向量数据库

| 数据库 | Star | 核心特点 |
|--------|------|---------|
| Milvus | 28.4K | 企业级，支持多种索引和GPU加速 |
| Qdrant | 18.9K | Rust实现，性能高，支持过滤 |
| Chroma | 13.7K | 轻量，适合原型开发 |
| Weaviate | 10.4K | GraphQL API，支持混合检索 |

##### 6.3.6 检索结果重排（Reranking）

| 方法 | 代表工作 | 原理 |
|------|---------|------|
| 交叉编码器 | MiniLM-L6 | 查询-文档对联合编码打分 |
| 上下文学习重排 | RankGPT | 用GPT对候选文档排序（滑动窗口处理长列表） |

#### 6.4 生成增强策略

##### 6.4.1 何时增强？-- 内部知识判断

| 方法类别 | 具体技术 | 评估维度 |
|---------|---------|---------|
| 外部观测法 | 直接询问/多次询问一致性 | 可靠性较低，模型可能过度自信 |
| | 实体流行度（Wikipedia页面浏览量） | 可操作，但依赖数据时效 |
| 内部观测法 | 探针（Probe）：训练线性分类器分析中间层隐藏状态 | 更精确，但仅限白盒模型 |

> **最佳实践**：对流行知识（高频训练数据覆盖）不增强，对长尾/时效知识增强。

##### 6.4.2 增强位置选择

| 位置 | 方法 | 优点 | 缺点 |
|------|------|------|------|
| 输入端 | Prompt拼接 | 简单、兼容黑盒 | 受上下文长度限制 |
| 中间层 | 交叉注意力融合（如RETRO） | 深入影响内部表示 | 需修改模型架构 |
| 输出端 | 后校验/后矫正（如REFEED） | 灵活性高 | 依赖检索质量 |

##### 6.4.3 多次增强策略

| 策略 | 框架 | 流程 |
|------|------|------|
| **分解式增强**（复杂问题） | DSP | 问题分解 -> 子问题迭代检索 -> 综合回答 |
| **渐进式增强**（模糊问题） | TOC | 多路径澄清 -> 递归检索细化 -> 树状答案路径 |

##### 6.4.4 降本增效

| 方法 | 代表工作 | 原理 |
|------|---------|------|
| Token级压缩 | LongLLMLingua | 困惑度评估Token重要性，去低信息量Token |
| 子文本级过滤 | FIT-RAG | 双标签打分器（事实性+模型偏好度） |
| 全文本信息提取 | PRCA | 强化学习训练信息提取器 |
| KV-Cache复用 | RAGCache | 多级动态缓存+PGDSF替换策略 |

#### 6.5 RAG实践与应用

##### 6.5.1 快速搭建（LangChain示例）

```
核心组件链：
DocumentLoader -> TextSplitter -> Embeddings + VectorStore
-> Retriever -> Prompt + LLM -> OutputParser
```

- **LangChain**：全栈LLM应用框架，模块化组装
- **LlamaIndex**：专注索引与检索，大数据场景效率更高

##### 6.5.2 典型应用

**（1）智能体（Agent）**：RAG贯穿记忆、计划、行动三个模块
- 记忆：检索历史交互 -> 辅助记忆更新
- 计划：检索相关信息 -> 辅助任务分解
- 行动：检索外部知识 -> 辅助决策执行

**（2）多模态垂域RAG**：
- 医疗领域：X光片+病历 -> 多模态检索 -> 辅助诊断
- 金融领域：财报+研报 -> 知识检索 -> 智能问答
- 生物学：蛋白质序列+文献 -> 跨模态检索

---

## 三、关键概念交叉索引表

| 概念 | 定义 | 出处 |
|------|------|------|
| Perplexity (PPL) | 语言模型的困惑度，衡量预测质量 | 第1章 |
| Scaling Laws | 模型性能与参数量/数据/计算量的幂律关系 | 第2章 |
| Emergence | 能力在大规模时突然涌现 | 第2章 |
| In-Context Learning | 通过Prompt中的示例在推理时临时学习 | 第3章 |
| Chain-of-Thought | 引导模型逐步推理的Prompt技巧 | 第3章 |
| LoRA | 对权重增量做低秩分解的参数高效微调 | 第4章 |
| Causal Tracing | 通过破坏-恢复实验定位知识存储神经元 | 第5章 |
| ROME | 定位+编辑特定事实的模型编辑方法 | 第5章 |
| Cross-Encoder | 查询-文档联合编码的相关性评估模型 | 第6章 |
| Bi-Encoder | 查询和文档分别编码的稠密检索模型 | 第6章 |

---

## 四、实操方法论与技巧

### 1. 选型决策速查表

| 需求场景 | 推荐方案 |
|---------|---------|
| 对话/聊天机器人 | Decoder-only模型（GPT/LLaMA）+ LoRA微调 |
| 文本分类/NER | Encoder-only模型（BERT/RoBERTa）+ 分类头 |
| 翻译/摘要 | Encoder-Decoder模型（T5/BART） |
| 少数据场景 | Prompt工程 + ICL |
| 中等数据场景（100-10K） | LoRA / Adapter微调 |
| 大数据场景（>10K） | 全参数微调或大规模LoRA |
| 知识时效性要求高 | RAG检索增强 |
| 需要更新特定知识 | 模型编辑（ROME/MEMIT） |

### 2. Prompt工程最佳实践

```
1. 先写指令，再放示例，最后提问
2. 示例数3-5个最优（太少不可靠，太多超长度）
3. 高频类放前面（模型对Prompt尾部注意力更高）
4. 对于推理任务，至少一个示例带有完整推理步骤
5. 利用分隔符（###、---）清晰划分区块
```

### 3. PEFT实操工作流

```
步骤1：确定基座模型（推荐LLaMA/Qwen/ChatGLM等开源模型）
步骤2：安装HF PEFT + bitsandbytes（量化加载）
步骤3：确定PEFT方法（默认选LoRA，秩r=16，alpha=32）
步骤4：准备指令微调数据集（通常1K-10K条）
步骤5：训练 -> 评估 -> 合并权重 -> 推理
```

### 4. RAG调优清单

| 优化环节 | 具体手段 |
|---------|---------|
| 知识库 | 文档清洗、去重、分块（Chunk Size 500-1000）、重叠10-20% |
| 检索 | 混合检索（稀疏BM25+稠密DPR）、查询改写、HyDE假设文档 |
| 重排 | MiniLM交叉编码器 或 RankGPT |
| 增强 | 判断是否需要增强、选择增强位置、必要时多轮迭代 |
| 压缩 | LongLLMLingua去除冗余Token、FIT-RAG过滤无用子文档 |

---

## 五、重点精华摘录

> "语言模型的本质是对自然语言序列的概率分布进行建模--看似简单的定义，却催生了整个NLP的革命。"（第1章）

> "缩放法则告诉我们，模型的智慧不是线性堆积的，而是在某个临界规模突然涌现的。"（第2章）

> "In-Context Learning是大语言模型最具魔力的能力之一：模型在推理时，仅凭几个示例就能学会新任务，而无需任何梯度更新。"（第3章）

> "Chain-of-Thought的价值不在于给出正确答案，而在于让你能看到模型是如何思考的。"（第3章）

> "LoRA的核心洞见：模型微调时的权重增量DeltaW天然具有低秩结构，因此可以用两个小矩阵来逼近，从而将可训练参数量压缩到原模型的千分之一。"（第4章）

> "因果跟踪实验揭示了一个深刻的事实：大语言模型的知识并非均匀分布，而是集中在中间层MLP的特定神经元中--这为精准编辑打开了一扇窗。"（第5章）

> "RAG不是一个大模型能力的补充，而是解决大模型"知其然不知其所以然"问题的根本方案--让模型学会查资料比让它记住所有东西更现实、更高效。"（第6章）

> "判断是否需要检索增强本身就是一门艺术：盲目增强不仅增加计算成本，还可能因为噪音文档而画蛇添足。"（第6章）

---

## 六、学习路径建议

### 第一阶段：基础牢筑（第1章 + Transformer原论文）
- **目标**：理解语言模型的数学根基，掌握Transformer架构
- **检验**：手写一个简单的前馈语言模型训练代码，计算PPL

### 第二阶段：宏观把握（第2章）
- **目标**：了解GPT/BERT/LLaMA/MoE等主流架构的差异与演进逻辑
- **检验**：能清晰解释"为什么Decoder-only成为主流"

### 第三阶段：应用上手（第3章 + 第6章）
- **目标**：掌握Prompt工程和RAG搭建
- **检验**：用LangChain搭建一个本领域的RAG问答系统

### 第四阶段：进阶调优（第4章）
- **目标**：会用LoRA在自己的数据上微调开源模型
- **检验**：在自己的GPU上完成一次完整的LoRA微调实验

### 第五阶段：前沿探索（第5章）
- **目标**：理解模型编辑的原理和应用边界
- **检验**：用ROME修改模型中的一个过时知识，验证准确性和局部性

### 建议扩展阅读
- **Attention Is All You Need**（Vaswani et al., 2017）：Transformer原论文，必读
- **Scaling Laws for Neural Language Models**（Kaplan et al., 2020）：缩放法则奠基论文
- **LoRA: Low-Rank Adaptation of Large Language Models**（Hu et al., 2022）：PEFT必读
- **Locating and Editing Factual Associations in GPT**（Meng et al., 2022）：ROME原论文
- **Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks**（Lewis et al., 2020）：RAG开山论文

---

## 七、全书一句话总结

**从语言模型数学基础到RAG检索增强，本书提供了一条从"会用大模型"到"理解大模型"再到"驾驭大模型"的完整学习路径--六章内容层层递进，涵盖了2024年大模型从业者必备的核心知识图谱。**
*（内容由AI生成，仅供参考）*
